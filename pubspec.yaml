name: pavan_dev_portfolio
description: "A modern developer portfolio built using Flutter, showcasing my journey as a Senior Mobile App Developer. This project highlights my technical expertise, experience, and featured projects — available as both a mobile app and a single-page web application (SPA)."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
